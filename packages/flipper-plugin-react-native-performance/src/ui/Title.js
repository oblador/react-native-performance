import React from 'react';
import { styled, Text } from 'flipper';
import { COLOR_TITLE } from './constants';

export const Title = styled(Text)({
  fontSize: 24,
  color: COLOR_TITLE,
  fontWeight: 300,
});
